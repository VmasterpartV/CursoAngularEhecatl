export interface Usuario {
    id: number;
    nombre: string;
    direccion: string;
    fotoUrl: string;
}

export interface User {
    name: string;
    age: number;
    number: string;
    email: string;
    hasNumber: boolean;
}